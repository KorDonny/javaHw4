pipeline {
    agent any
    
    stages {
        
        stage('github-clone') {
            steps {
                git branch: 'BE', credentialsId: 'jenkins', url: '{https://github.com/KorDonny/javaHw4.git'
            }
        }
        
   		// stage...
   	}
}