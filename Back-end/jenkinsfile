pipeline {
    agent any
    
    stages {
        
        stage('github-clone') {
            steps {
                git branch: 'BE', credentialsId: 'jenkins', url: '{REPOSITORY URL}'
            }
        }
        
   		// stage...
   	}
}